[package]
name = "sw"
version = "0.8.2"
authors = ["Ula Shipman <ula.hello@mailbox.org>"]
edition = "2021"
description = "terminal stopwatch"
license = "GPL-3.0-or-later"

[dependencies]
libsw = "0"
log = "0.4"
termcolor = "1.1"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
