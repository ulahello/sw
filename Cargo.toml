[package]
name = "sw"
version = "0.8.3"
authors = ["Ula Shipman <ula.hello@mailbox.org>"]
edition = "2021"
description = "terminal stopwatch"
license = "GPL-3.0-or-later"

[dependencies]
libsw = "1"
log = "0"
termcolor = "1"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
