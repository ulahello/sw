[package]
name = "sw"
version = "0.8.1"
authors = ["Ula Shipman <ula.hello@mailbox.org>"]
edition = "2021"
description = "terminal stopwatch"
license = "GPL-3.0-or-later"

[dependencies]
log = "0.4"
termcolor = "1.1"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
